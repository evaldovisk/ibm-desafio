spring:
  application:
    name: desafio-ibm
  datasource:
    url: jdbc:postgresql://postgres:5432/ibm-desafio-db
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true

